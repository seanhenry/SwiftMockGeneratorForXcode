import Foundation
import SourceKittenFramework


class SSI {
    
    func findParentClass(contents: String, cursorOffset: Int) -> Element? {
        let structure = Structure(file: File(contents: contents))
        return nil
    }
}
